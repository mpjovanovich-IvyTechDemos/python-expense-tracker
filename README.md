# Expense Tracker

This project shows how multiple Python files are combined to form a larger program.

It uses class files to **encapsulate** functionality - to organize it into cohesive groupings.

The main program file **imports** these classes to get access to the code in them, then it can use them like normal.
